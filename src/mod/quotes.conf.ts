export const conf = {
    // quotes update interval in milliseconds.
    // 1000 milliseconds = 1 second.
    updateInterval: 90_000,

    // quotes source file.
    // can be either a local path or a url to a cdn.
    quotesFile: 'https://cdn.jsdelivr.net/gh/etrusci-org/quotes@main/js/quotes-s9.min.js',

    // speed of the typing effect in milliseconds.
    // 1000 milliseconds = 1 second.
    typingSpeed: 80,
}
