export const modConf = {
    /*
    some update rate in milliseconds.
    1000 milliseconds = 1 second.
    */
    updaterate: '1000',

    /*
    some param.
    override with request params in the url, e.g. mod=example&message=test one two<br>foo bar
    */
    message: 'hello cruel world',
}
